Guidelines per la modifica dei css

Norme generali
 - Nei css salvare solo i parametri modificati rispetto al tema di base.


Utilizzo dei singoli file css
 - regions.css
   Impostare solo gli attributi relativi alle regioni ed alle loro sezioni interne (inner) come margini, padding, sfondi.

